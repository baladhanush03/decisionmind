# Production environment — Vite bakes this into the build at compile time
# Replace this URL with your actual Render.com backend URL after deployment
VITE_API_URL=https://decisionmind-backend.onrender.com/api/v1
